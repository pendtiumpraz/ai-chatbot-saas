import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { ThemeProvider } from '@/components/theme-provider'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Universal AI Chatbot - Multi-Industry SaaS Platform',
  description: 'Deploy intelligent AI chatbots with RAG for any industry. Customer service, education, healthcare, finance, and more.',
  keywords: ['AI chatbot', 'RAG', 'SaaS', 'LangChain', 'automation', 'customer service'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
